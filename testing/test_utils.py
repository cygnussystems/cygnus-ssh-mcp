# This file is kept for backward compatibility
# Import everything from conftest.py
from conftest import (
    SSH_HOST, SSH_PORT, SSH_USER, SSH_PASSWORD, SSH_KEYFILE, 
    TEST_SUDO_PASSWORD, get_client, cleanup_client, 
    print_test_header, print_test_footer
)
