ok, we gave a core SSH component (SshClient) that can connect via SSH and do things on
a remote computer.
We also have an MCP wrapper in mcp_ssh_server.py .

In the 'testing' directory we tested the SshClient Extensively.


In the 'testing_mcp' directory , we now want to test the MCP wrapper
extensively as well. in the testing_mcp\conftest.py we haev various
fixtures for connecting to a docker container.

The other files in 'testing_mcp' aren't current working unfortunately.

The current task is to fix this. I want to start by implementing a minimalist
MCP server being called from an MCP client through which we call the
'tools' offered by the MCP server. How to do this is documented in
doc\mcp_server_testing_demo.py


I do not currently understand why we can't get it to work. This is why we
need to start with a minimalist setup. 